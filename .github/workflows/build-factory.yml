# Copyright (c) 2018-2019 The Veil developers
# Distributed under the MIT software license, see the accompanying
# file COPYING or http://www.opensource.org/licenses/mit-license.php.
name: Github Actions CI for Veil
on: [push, pull_request, workflow_dispatch]
env:
  SOURCE_ARTIFACT: source
jobs:
  create-source-distribution:
    name: Create Source Distribution
    runs-on: ubuntu-22.04
    env:
      ARTIFACT_DIR: source
    steps:
    - name: Checkout
      uses: actions/checkout@v5.0.0
    # Needs qt and protobuf to configure qt and include veil-qt.1 in distribution
    - name: Install Required Packages
      run: |
        sudo apt update
        sudo apt install -y libboost-all-dev libdb-dev libdb++-dev libevent-dev libqt5gui5 libqt5core5a libqt5dbus5 qttools5-dev qttools5-dev-tools libprotobuf-dev protobuf-compiler
    - name: Create Distribution Tarball
      run: |
        ./autogen.sh
        ./configure --with-incompatible-bdb
        make dist
    - name: Download Dependencies
      run: make -C depends download
    - name: Create Dependencies Tarball
      run: tar -czf depends.tar.gz depends
    - name: Prepare Files for Artifact
      run: |
        mkdir -p $ARTIFACT_DIR
        mv depends.tar.gz veil-*.tar.gz $ARTIFACT_DIR
    - name: Upload Artifact
      uses: actions/upload-artifact@v6.0.0
      with:
        name: ${{ env.SOURCE_ARTIFACT }}
        path: ${{ env.ARTIFACT_DIR }}
  build-x86_64-linux:
    name: Build for x86 Linux 64bit
    needs: create-source-distribution
    runs-on: ubuntu-22.04
    env:
      ARTIFACT_DIR: x86_64-linux-binaries
      TEST_LOG_ARTIFACT_DIR: test-logs
    steps:
    - name: Getting Source
      uses: actions/download-artifact@v7.0.0
      with:
        name: ${{ env.SOURCE_ARTIFACT }}
        path: ${{ env.SOURCE_ARTIFACT }}
    - name: Extract Archives
      run: |
        tar -xzf depends.tar.gz
        tar -xzf veil-*.tar.gz --strip-components=1
      working-directory: ${{ env.SOURCE_ARTIFACT }}
    - name: Install Required Packages
      run: |
        sudo apt update
        sudo apt install -y python3-zmq dpkg-dev imagemagick
    - name: Build Dependencies
      run: make -C depends -j$(nproc)
      working-directory: ${{ env.SOURCE_ARTIFACT }}
    - name: Build Veil
      run: |
        ./configure --disable-jni --enable-tests --with-comparison-tool=no --prefix=$(realpath depends/x86_64-pc-linux-gnu)
        make -j$(nproc)
      working-directory: ${{ env.SOURCE_ARTIFACT }}
    - name: Verify daemon binaries link without OpenSSL
      # 1.5.0 removed OpenSSL from veild/veil-cli/veil-tx (the RNG was its
      # last user). Guard against it creeping back in: ldd catches dynamic
      # linkage, nm on the still-unstripped binaries catches statically
      # linked-in libssl/libcrypto from depends. veil-qt is intentionally
      # not checked - the GUI still links OpenSSL via its Qt/BIP70 lineage.
      run: |
        for bin in src/veild src/veil-cli src/veil-tx; do
          if ldd "$bin" | grep -Ei 'libssl|libcrypto'; then
            echo "::error::$bin dynamically links OpenSSL"; exit 1
          fi
          if nm --defined-only "$bin" 2>/dev/null | grep -Ew 'SSL_new|SSL_CTX_new|EVP_EncryptInit_ex|RAND_bytes'; then
            echo "::error::$bin contains statically linked OpenSSL symbols"; exit 1
          fi
          echo "$bin: no OpenSSL linkage"
        done
      working-directory: ${{ env.SOURCE_ARTIFACT }}
    - name: Prepare Files for Artifact
      run: |
        mkdir -p $ARTIFACT_DIR
        strip $SOURCE_ARTIFACT/src/{veil-cli,veil-tx,veild,qt/veil-qt}
        mv $SOURCE_ARTIFACT/src/{veil-cli,veil-tx,veild,qt/veil-qt} $ARTIFACT_DIR
    # Upload the binaries before packaging runs. Packaging is the newest and
    # most fragile part of this job, and it should never be able to cost us the
    # binaries that already built cleanly.
    - name: Upload Artifact
      uses: actions/upload-artifact@v6.0.0
      with:
        name: ${{ env.ARTIFACT_DIR }}
        path: ${{ env.ARTIFACT_DIR }}
    # Builds a .deb so Debian/Ubuntu/Mint users get a double click install and a
    # Veil entry in the application menu instead of a folder of loose binaries.
    # Skipped for pull requests and branch pushes, where it only costs time.
    # Master still runs it on every push, so a break shows up while it is cheap
    # to fix rather than when a release tag is waiting on it.
    - name: Build Debian Package
      if: >-
        github.event_name == 'workflow_dispatch' ||
        startsWith(github.ref, 'refs/tags/') ||
        github.ref == 'refs/heads/master'
      run: |
        VERSION=$(ls $SOURCE_ARTIFACT/veil-*.tar.gz | sed 's|.*/veil-||; s|\.tar\.gz$||')
        PKG=veil-deb
        mkdir -p $PKG/DEBIAN $PKG/usr/bin $PKG/usr/share/applications \
                 $PKG/usr/share/pixmaps $PKG/usr/share/doc/veil
        cp $ARTIFACT_DIR/{veil-cli,veil-tx,veild,veil-qt} $PKG/usr/bin
        cp $SOURCE_ARTIFACT/COPYING $PKG/usr/share/doc/veil/copyright
        ICON=$SOURCE_ARTIFACT/src/qt/res/icons/bitcoin.png
        for SIZE in 32 64 128 256; do
          mkdir -p $PKG/usr/share/icons/hicolor/${SIZE}x${SIZE}/apps
          convert $ICON -resize ${SIZE}x${SIZE} $PKG/usr/share/icons/hicolor/${SIZE}x${SIZE}/apps/veil.png
        done
        cp $PKG/usr/share/icons/hicolor/256x256/apps/veil.png $PKG/usr/share/pixmaps/veil.png
        cat > $PKG/usr/share/applications/veil-qt.desktop <<'DESKTOP'
        [Desktop Entry]
        Type=Application
        Name=Veil
        GenericName=Veil Wallet
        Comment=Send and receive Veil
        Exec=veil-qt %u
        Icon=veil
        Terminal=false
        Categories=Office;Finance;
        MimeType=x-scheme-handler/veil;
        DESKTOP
        # Read the real shared library deps off the binary instead of guessing
        mkdir -p debian
        printf 'Source: veil\n\nPackage: veil\nArchitecture: amd64\n' > debian/control
        DEPENDS=$(dpkg-shlibdeps --ignore-missing-info -O $PKG/usr/bin/veil-qt 2>/dev/null | sed 's/^shlibs:Depends=//') || true
        [ -n "$DEPENDS" ] || DEPENDS='libc6, libstdc++6, libgcc-s1'
        # dpkg-deb does not work this out on its own, and without it apt reports
        # "0 B of additional disk space will be used" and cannot warn on a full disk
        INSTALLED_SIZE=$(du -ks $PKG/usr | cut -f1)
        cat > $PKG/DEBIAN/control <<CONTROL
        Package: veil
        Version: $VERSION
        Architecture: amd64
        Installed-Size: $INSTALLED_SIZE
        Section: utils
        Priority: optional
        Maintainer: Veil Project <support@veil-project.com>
        Homepage: https://veil-project.com/
        Depends: $DEPENDS
        Description: Veil privacy focused cryptocurrency wallet
         Veil is a privacy focused cryptocurrency built on RingCT and zerocoin.
         This package installs the Veil-Qt wallet along with the veild daemon
         and the veil-cli and veil-tx command line tools.
        CONTROL
        mkdir -p installers
        dpkg-deb --build --root-owner-group $PKG installers/veil-$VERSION-amd64.deb
    - name: Upload Installer
      if: >-
        github.event_name == 'workflow_dispatch' ||
        startsWith(github.ref, 'refs/tags/') ||
        github.ref == 'refs/heads/master'
      uses: actions/upload-artifact@v6.0.0
      with:
        name: installers-x86_64-linux
        path: installers
  build-win64:
    name: Build for Win64
    needs: create-source-distribution
    runs-on: ubuntu-22.04
    env:
      ARTIFACT_DIR: win64-binaries
    steps:
    - name: Getting Source
      uses: actions/download-artifact@v7.0.0
      with:
        name: ${{ env.SOURCE_ARTIFACT }}
        path: ${{ env.SOURCE_ARTIFACT }}
    - name: Extract Archives
      run: |
        tar -xzf depends.tar.gz
        tar -xzf veil-*.tar.gz --strip-components=1
      working-directory: ${{ env.SOURCE_ARTIFACT }}
    - name: Install Required Packages
      run: |
        sudo apt update
        sudo apt install -y g++-mingw-w64-x86-64 gcc-mingw-w64-x86-64 nsis
    - name: Switch MinGW GCC and G++ to POSIX Threading
      run: |
        sudo update-alternatives --set x86_64-w64-mingw32-gcc /usr/bin/x86_64-w64-mingw32-gcc-posix
        sudo update-alternatives --set x86_64-w64-mingw32-g++ /usr/bin/x86_64-w64-mingw32-g++-posix
    - name: Build Dependencies
      run: make -C depends -j$(nproc) HOST=x86_64-w64-mingw32
      working-directory: ${{ env.SOURCE_ARTIFACT }}
    - name: Build Veil
      run: |
        ./configure --disable-jni --prefix=$(realpath depends/x86_64-w64-mingw32)
        make -j$(nproc)
      working-directory: ${{ env.SOURCE_ARTIFACT }}
    # Copy rather than move: the installer is built from the binaries in src/,
    # so they have to stay there, and getting them uploaded first means a
    # packaging break cannot take the binaries down with it.
    - name: Prepare Files for Artifact
      run: |
        mkdir -p $ARTIFACT_DIR
        strip $SOURCE_ARTIFACT/src/{veil-cli.exe,veil-tx.exe,veild.exe,qt/veil-qt.exe}
        cp $SOURCE_ARTIFACT/src/{veil-cli.exe,veil-tx.exe,veild.exe,qt/veil-qt.exe} $ARTIFACT_DIR
    - name: Upload Artifact
      uses: actions/upload-artifact@v6.0.0
      with:
        name: ${{ env.ARTIFACT_DIR }}
        path: ${{ env.ARTIFACT_DIR }}
    # Produces veil-<version>-win64-setup.exe from share/setup.nsi: start menu
    # entries, the veil: URI handler and an uninstaller, same as any other
    # Windows program. Skipped for pull requests and branch pushes; master still
    # builds it on every push so breakage surfaces before a tag needs it.
    - name: Build Windows Installer
      if: >-
        github.event_name == 'workflow_dispatch' ||
        startsWith(github.ref, 'refs/tags/') ||
        github.ref == 'refs/heads/master'
      run: |
        (cd $SOURCE_ARTIFACT && make deploy)
        mkdir -p installers
        mv $SOURCE_ARTIFACT/veil-*-win64-setup.exe installers
    - name: Upload Installer
      if: >-
        github.event_name == 'workflow_dispatch' ||
        startsWith(github.ref, 'refs/tags/') ||
        github.ref == 'refs/heads/master'
      uses: actions/upload-artifact@v6.0.0
      with:
        name: installers-win64
        path: installers
  build-osx64:
    name: Build for MacOSX
    needs: create-source-distribution
    runs-on: ubuntu-22.04
    env:
      ARTIFACT_DIR: macosx-x86_64-binaries
    steps:
    - name: Getting Source
      uses: actions/download-artifact@v7.0.0
      with:
        name: ${{ env.SOURCE_ARTIFACT }}
        path: ${{ env.SOURCE_ARTIFACT }}

    - name: Extract Archives
      run: |
        tar -xzf depends.tar.gz
        tar -xzf veil-*.tar.gz --strip-components=1
      working-directory: ${{ env.SOURCE_ARTIFACT }}

    - name: Install Required Packages
      run: |
        sudo apt update
        sudo apt install -y python3-setuptools libcap-dev zlib1g-dev cmake \
          genisoimage librsvg2-bin imagemagick libtiff-tools
        sudo -H pip install setuptools
        # macdeployqtplus and custom_dsstore.py need these to lay out the .dmg,
        # installed against the same python3 that configure will pick up
        sudo -H python3 -m pip install ds_store mac_alias biplist

    - name: Work around missing libtinfo.so.5 for macOS cross-toolchain
      run: |
        # Try to install libtinfo5 if it's in the repo
        if apt-cache show libtinfo5 >/dev/null 2>&1; then
          sudo apt-get install -y libtinfo5
        fi
        # If still no libtinfo.so.5, symlink it to libtinfo.so.6
        if [ ! -f /usr/lib/x86_64-linux-gnu/libtinfo.so.5 ] && [ -f /usr/lib/x86_64-linux-gnu/libtinfo.so.6 ]; then
          echo "Creating libtinfo.so.5 -> libtinfo.so.6 symlink..."
          sudo ln -s /usr/lib/x86_64-linux-gnu/libtinfo.so.6 /usr/lib/x86_64-linux-gnu/libtinfo.so.5
        fi

    - name: Get macOS SDK
      run: |
        mkdir -p depends/sdk-sources
        mkdir -p depends/SDKs
        curl https://bitcoincore.org/depends-sources/sdks/Xcode-11.3.1-11C505-extracted-SDK-with-libcxx-headers.tar.gz -o depends/sdk-sources/Xcode-11.3.1-11C505-extracted-SDK-with-libcxx-headers.tar.gz
        tar -C depends/SDKs -xf depends/sdk-sources/Xcode-11.3.1-11C505-extracted-SDK-with-libcxx-headers.tar.gz
      working-directory: ${{ env.SOURCE_ARTIFACT }}

    - name: Build Dependencies
      run: make -C depends HOST=x86_64-apple-darwin18 -j$(nproc)
      working-directory: ${{ env.SOURCE_ARTIFACT }}

    - name: Build Veil
      run: |
        ./configure --enable-c++17 --disable-jni --prefix=$(realpath depends/x86_64-apple-darwin18)
        make -j$(nproc)
      working-directory: ${{ env.SOURCE_ARTIFACT }}

    # Copy rather than move: the disk image is built from the tree in src/, so
    # the binaries have to stay put, and uploading them first means a packaging
    # break cannot take them down with it.
    - name: Prepare Files for Artifact
      run: |
        mkdir -p $ARTIFACT_DIR
        # strip fails with "Unable to recognise the format of the input file"
        #strip $SOURCE_ARTIFACT/src/{veil-cli,veil-tx,veild,qt/veil-qt}
        cp $SOURCE_ARTIFACT/src/{veil-cli,veil-tx,veild,qt/veil-qt} $ARTIFACT_DIR

    - name: Upload Artifact
      uses: actions/upload-artifact@v6.0.0
      with:
        name: ${{ env.ARTIFACT_DIR }}
        path: ${{ env.ARTIFACT_DIR }}

    # Wraps Veil-Qt.app in the usual drag-to-Applications disk image. The .dmg
    # is unsigned, so first launch is right click -> Open. Skipped for pull
    # requests and branch pushes; master still builds it on every push so
    # breakage surfaces before a tag needs it.
    - name: Build Disk Image
      if: >-
        github.event_name == 'workflow_dispatch' ||
        startsWith(github.ref, 'refs/tags/') ||
        github.ref == 'refs/heads/master'
      run: |
        (cd $SOURCE_ARTIFACT && make deploy)
        mkdir -p installers
        VERSION=$(ls $SOURCE_ARTIFACT/veil-*.tar.gz | sed 's|.*/veil-||; s|\.tar\.gz$||')
        # One dmg is expected; name it explicitly rather than globbing into a
        # single destination, which would fail confusingly if that ever changed.
        DMG=$(ls $SOURCE_ARTIFACT/*.dmg | head -n1)
        mv "$DMG" installers/veil-$VERSION-osx64.dmg

    - name: Upload Installer
      if: >-
        github.event_name == 'workflow_dispatch' ||
        startsWith(github.ref, 'refs/tags/') ||
        github.ref == 'refs/heads/master'
      uses: actions/upload-artifact@v6.0.0
      with:
        name: installers-osx64
        path: installers
  build-aarch64-linux:
    name: Build for ARM Linux 64bit
    needs: create-source-distribution
    runs-on: ubuntu-22.04
    env:
      ARTIFACT_DIR: aarch64-linux-binaries
    steps:
    - name: Getting Source
      uses: actions/download-artifact@v7.0.0
      with:
        name: ${{ env.SOURCE_ARTIFACT }}
        path: ${{ env.SOURCE_ARTIFACT }}
    - name: Extract Archives
      run: |
        tar -xzf depends.tar.gz
        tar -xzf veil-*.tar.gz --strip-components=1
      working-directory: ${{ env.SOURCE_ARTIFACT }}
    - name: Install Required Packages
      run: |
        sudo apt update
        sudo apt install -y python3-zmq libcap-dev cmake g++-aarch64-linux-gnu
    - name: Build Dependencies
      run: make -C depends HOST=aarch64-linux-gnu -j$(nproc)
      working-directory: ${{ env.SOURCE_ARTIFACT }}
    - name: Build Veil
      run: |
        ./configure --disable-jni --prefix=$(realpath depends/aarch64-linux-gnu)
        make -j$(nproc)
      working-directory: ${{ env.SOURCE_ARTIFACT }}
    - name: Prepare Files for Artifact
      run: |
        mkdir -p $ARTIFACT_DIR
        # strip fails with "Unable to recognise the format of the input file"
        # strip $SOURCE_ARTIFACT/src/{veil-cli,veil-tx,veild,qt/veil-qt}
        mv $SOURCE_ARTIFACT/src/{veil-cli,veil-tx,veild,qt/veil-qt} $ARTIFACT_DIR
    - name: Upload Artifact
      uses: actions/upload-artifact@v6.0.0
      with:
        name: ${{ env.ARTIFACT_DIR }}
        path: ${{ env.ARTIFACT_DIR }}
  build-arm-linux-gnueabihf:
    name: Build for ARM Linux 32bit
    needs: create-source-distribution
    runs-on: ubuntu-22.04
    env:
      ARTIFACT_DIR: arm32-binaries
    steps:
    - name: Getting Source
      uses: actions/download-artifact@v7.0.0
      with:
        name: ${{ env.SOURCE_ARTIFACT }}
        path: ${{ env.SOURCE_ARTIFACT }}
    - name: Extract Archives
      run: |
        tar -xzf depends.tar.gz
        tar -xzf veil-*.tar.gz --strip-components=1
      working-directory: ${{ env.SOURCE_ARTIFACT }}
    - name: Install Required Packages
      run: |
        sudo apt update
        sudo apt install -y g++-arm-linux-gnueabihf libgmp-dev
    - name: Build Dependencies
      run: make -C depends -j$(nproc) HOST=arm-linux-gnueabihf
      working-directory: ${{ env.SOURCE_ARTIFACT }}
    - name: Build Veil
      run: |
        ./configure --disable-jni --prefix=$(realpath depends/arm-linux-gnueabihf) --enable-glibc-back-compat --enable-reduce-exports LDFLAGS=-static-libstdc++
        make -j$(nproc)
      working-directory: ${{ env.SOURCE_ARTIFACT }}
    - name: Prepare Files for Artifact
      run: |
        mkdir -p $ARTIFACT_DIR
        # strip fails with "Unable to recognise the format of the input file"
        # strip $SOURCE_ARTIFACT/src/{veil-cli,veil-tx,veild,qt/veil-qt}
        mv $SOURCE_ARTIFACT/src/{veil-cli,veil-tx,veild,qt/veil-qt} $ARTIFACT_DIR
    - name: Upload Artifact
      uses: actions/upload-artifact@v6.0.0
      with:
        name: ${{ env.ARTIFACT_DIR }}
        path: ${{ env.ARTIFACT_DIR }}
  build-i686-linux32:
    name: Build for x86 Linux 32bit
    needs: create-source-distribution
    runs-on: ubuntu-22.04
    env:
      ARTIFACT_DIR: i686-linux32-binaries
    steps:
    - name: Getting Source
      uses: actions/download-artifact@v7.0.0
      with:
        name: ${{ env.SOURCE_ARTIFACT }}
        path: ${{ env.SOURCE_ARTIFACT }}
    - name: Extract Archives
      run: |
        tar -xzf depends.tar.gz
        tar -xzf veil-*.tar.gz --strip-components=1
      working-directory: ${{ env.SOURCE_ARTIFACT }}
    - name: Install Required Packages
      run: |
        sudo apt update
        sudo apt install -y python3-zmq gcc-multilib g++-multilib
    - name: Build Dependencies
      run: make -C depends -j$(nproc) HOST=i686-linux-gnu
      working-directory: ${{ env.SOURCE_ARTIFACT }}
    - name: Build Veil
      run: |
        ./configure --disable-jni --prefix=$(realpath depends/i686-linux-gnu)
        make -j$(nproc)
      working-directory: ${{ env.SOURCE_ARTIFACT }}
    - name: Prepare Files for Artifact
      run: |
        mkdir -p $ARTIFACT_DIR
        strip $SOURCE_ARTIFACT/src/{veil-cli,veil-tx,veild,qt/veil-qt}
        mv $SOURCE_ARTIFACT/src/{veil-cli,veil-tx,veild,qt/veil-qt} $ARTIFACT_DIR
    - name: Upload Artifact
      uses: actions/upload-artifact@v6.0.0
      with:
        name: ${{ env.ARTIFACT_DIR }}
        path: ${{ env.ARTIFACT_DIR }}
  build-riscv64-linux:
    name: Build for RISCV Linux 64bit
    needs: create-source-distribution
    runs-on: ubuntu-22.04
    env:
      ARTIFACT_DIR: riscv64-linux-binaries
    steps:
    - name: Getting Source
      uses: actions/download-artifact@v7.0.0
      with:
        name: ${{ env.SOURCE_ARTIFACT }}
        path: ${{ env.SOURCE_ARTIFACT }}
    - name: Extract Archives
      run: |
        tar -xzf depends.tar.gz
        tar -xzf veil-*.tar.gz --strip-components=1
      working-directory: ${{ env.SOURCE_ARTIFACT }}
    - name: Install Required Packages
      run: |
        sudo apt update
        sudo apt install -y python3-zmq libcap-dev cmake g++-riscv64-linux-gnu
    - name: Build Dependencies
      run: make -C depends NO_QR=1 NO_QT=1 HOST=riscv64-linux-gnu -j$(nproc)
      working-directory: ${{ env.SOURCE_ARTIFACT }}
    - name: Build Veil
      run: |
        ./configure --disable-jni --without-gui --prefix=$(realpath depends/riscv64-linux-gnu)
        make -j$(nproc)
      working-directory: ${{ env.SOURCE_ARTIFACT }}
    - name: Prepare Files for Artifact
      run: |
        mkdir -p $ARTIFACT_DIR
        # strip fails with "Unable to recognise the format of the input file"
        # strip $SOURCE_ARTIFACT/src/{veil-cli,veil-tx,veild}
        mv $SOURCE_ARTIFACT/src/{veil-cli,veil-tx,veild} $ARTIFACT_DIR
    - name: Upload Artifact
      uses: actions/upload-artifact@v6.0.0
      with:
        name: ${{ env.ARTIFACT_DIR }}
        path: ${{ env.ARTIFACT_DIR }}
  attach-installers-to-release:
    name: Attach Installers to Release
    needs: [build-x86_64-linux, build-win64, build-osx64]
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-22.04
    permissions:
      contents: write
    steps:
    - name: Getting Installers
      uses: actions/download-artifact@v7.0.0
      with:
        path: artifacts
    - name: Attach to Release
      env:
        GH_TOKEN: ${{ github.token }}
        GH_REPO: ${{ github.repository }}
      run: |
        # The installers arrive in their own artifacts, separate from the loose
        # binaries, so packaging can fail without costing us a binaries upload.
        mkdir -p installers
        cp artifacts/installers-win64/*-win64-setup.exe installers
        cp artifacts/installers-osx64/*.dmg installers
        cp artifacts/installers-x86_64-linux/*.deb installers
        (cd installers && sha256sum * > SHA256SUMS.txt)
        # A tag can be pushed before its release page exists, so open a draft
        # when there is nothing to attach to yet
        gh release view "$GITHUB_REF_NAME" >/dev/null 2>&1 || \
          gh release create "$GITHUB_REF_NAME" --draft --title "$GITHUB_REF_NAME" --notes "Installers built by CI."
        gh release upload "$GITHUB_REF_NAME" installers/* --clobber
  test-sanitizers-linux:
    name: Unit Tests (ASan + UBSan, Linux)
    needs: create-source-distribution
    runs-on: ubuntu-22.04
    env:
      # Leak detection is off: Berkeley DB and some globals are reclaimed by the
      # OS at exit rather than freed explicitly. ASan is a hard gate: any real
      # memory error (use-after-free, OOB, etc.) aborts and fails the job.
      ASAN_OPTIONS: detect_leaks=0:halt_on_error=1:abort_on_error=1:detect_stack_use_after_return=1
      # UBSan is a hard gate (halt_on_error=1): the sanitizer-instrumented unit
      # suite (incl. progpow_tests, which drives the vendored ethash) runs clean
      # with zero runtime-error diagnostics, so no suppressions file is needed.
      # If a future change trips UBSan on an intentional vendored-crypto pattern,
      # add a targeted src/test/sanitizer_ubsan.supp entry rather than relaxing this.
      UBSAN_OPTIONS: print_stacktrace=1:halt_on_error=1
    steps:
    - name: Getting Source
      uses: actions/download-artifact@v7.0.0
      with:
        name: ${{ env.SOURCE_ARTIFACT }}
        path: ${{ env.SOURCE_ARTIFACT }}
    - name: Extract Archives
      run: |
        tar -xzf depends.tar.gz
        tar -xzf veil-*.tar.gz --strip-components=1
      working-directory: ${{ env.SOURCE_ARTIFACT }}
    - name: Install Required Packages
      run: |
        sudo apt update
        sudo apt install -y python3-zmq
    - name: Build Dependencies
      run: make -C depends -j$(nproc)
      working-directory: ${{ env.SOURCE_ARTIFACT }}
    - name: Configure with sanitizers
      run: |
        ./configure --disable-jni --without-gui --enable-tests --disable-bench \
          --with-sanitizers=address,undefined --disable-hardening \
          CXXFLAGS="-O1 -g -fno-omit-frame-pointer" \
          --prefix=$(realpath depends/x86_64-pc-linux-gnu)
      working-directory: ${{ env.SOURCE_ARTIFACT }}
    - name: Build Veil (instrumented)
      run: make -j$(nproc)
      working-directory: ${{ env.SOURCE_ARTIFACT }}
    - name: Run sanitized unit tests
      # Curated set of suites known to pass; excludes suites with pre-existing
      # stale-vector failures (bloom, sighash, key_io, netbase, ...) and
      # randomx_tests (its JIT executes generated code that ASan does not
      # instrument). Expand as the remaining suites are fixed.
      run: >
        src/test/test_veil --catch_system_errors=no
        --run_test=crypto_tests,random_tests,serialize_tests,streams_tests,prevector_tests,uint256_tests,allocator_tests,sanity_tests,scheduler_tests,bech32_tests,bip32_tests,net_tests,netbase_tests,addrman_tests,blockencodings_tests,denialofservice_tests,monthly_rewards_tests,progpow_tests,zerocoin_implementation_tests
      working-directory: ${{ env.SOURCE_ARTIFACT }}
  build-macos-arm64-native:
    name: Native Build + Tests (macOS arm64)
    runs-on: macos-14
    env:
      ARTIFACT_DIR: macosx-arm64-binaries
      # Quiet Homebrew's non-fatal env hints and the deprecation nags for the
      # two formulas below (both are intentional and still install).
      HOMEBREW_NO_ENV_HINTS: 1
      HOMEBREW_NO_INSTALLED_DEPENDENTS_CHECK: 1
    steps:
    - name: Checkout
      uses: actions/checkout@v5.0.0
    - name: Install Required Packages
      # Formula notes:
      #  - berkeley-db@4: deprecated upstream but REQUIRED - the wallet uses the
      #    BDB 4.8 on-disk format; a newer BDB would change wallet.dat.
      #  - boost@1.85: pinned because the tree's AX_BOOST_* autoconf macros
      #    cannot resolve Boost 1.90+ (the current unversioned formula), which
      #    fails configure with "Could not find a version of the library!".
      #    Removing this pin needs an ax_boost_base.m4 modernization (tracked).
      # The untrusted aws/tap is pre-installed on the runner image and unrelated
      # to our formulas; untap it to silence the tap-trust warning.
      run: |
        brew untap aws/tap 2>/dev/null || true
        # librsvg (rsvg-convert) is needed by `make deploy` to render the
        # .dmg background image; it is not preinstalled on the macOS runner.
        brew install autoconf automake libtool pkgconf berkeley-db@4 boost@1.85 gmp libevent zeromq miniupnpc qt@5 protobuf librsvg qrencode
    - name: Autogen
      run: ./autogen.sh
    - name: Configure
      # qt@5 is keg-only (like berkeley-db@4/boost@1.85 above), so it needs an
      # explicit pkgconfig path and --with-qt-bindir for moc/uic/rcc/lrelease.
      # --with-qrencode is explicit (not auto) so configure hard-fails if
      # libqrencode goes missing instead of silently shipping a QR-less GUI.
      run: >
        ./configure --with-gui=yes --with-qrencode --disable-bench
        BDB_LIBS="-L$(brew --prefix berkeley-db@4)/lib -ldb_cxx-4.8"
        BDB_CFLAGS="-I$(brew --prefix berkeley-db@4)/include"
        CPPFLAGS="-I$(brew --prefix gmp)/include -DBOOST_NO_CXX98_FUNCTION_BASE"
        LDFLAGS="-L$(brew --prefix gmp)/lib"
        PKG_CONFIG_PATH="$(brew --prefix openssl@3)/lib/pkgconfig:$(brew --prefix libevent)/lib/pkgconfig:$(brew --prefix zeromq)/lib/pkgconfig:$(brew --prefix qt@5)/lib/pkgconfig"
        --with-qt-bindir="$(brew --prefix qt@5)/bin"
        --with-boost="$(brew --prefix boost@1.85)"
    - name: Build
      run: make -j$(sysctl -n hw.ncpu)
    - name: Run unit tests
      # Same curated green subset as the sanitizer job. Native arm64 build,
      # which is not otherwise exercised (build-osx64 is cross-compiled from
      # Linux and runs no tests).
      run: >
        src/test/test_veil --catch_system_errors=no
        --run_test=crypto_tests,random_tests,serialize_tests,streams_tests,prevector_tests,uint256_tests,allocator_tests,sanity_tests,scheduler_tests,bech32_tests,bip32_tests,net_tests,netbase_tests,addrman_tests,blockencodings_tests,denialofservice_tests,monthly_rewards_tests,progpow_tests,zerocoin_implementation_tests
    - name: Verify daemon binaries link without OpenSSL
      # Homebrew libs are all dynamic here, so otool -L is a complete check
      # that veild/veil-cli/veil-tx carry no libssl/libcrypto (removed with
      # the 1.5.0 RNG). veil-qt is intentionally not checked - the GUI still
      # links OpenSSL via its Qt/BIP70 lineage.
      run: |
        for bin in src/veild src/veil-cli src/veil-tx; do
          if otool -L "$bin" | grep -Ei 'libssl|libcrypto'; then
            echo "::error::$bin links OpenSSL"; exit 1
          fi
          echo "$bin: no OpenSSL linkage"
        done
    - name: Prepare Files for Artifact
      run: |
        mkdir -p $ARTIFACT_DIR
        strip src/veil-cli src/veil-tx src/veild
        mv src/veil-cli src/veil-tx src/veild src/qt/veil-qt $ARTIFACT_DIR
    - name: Upload Artifact
      uses: actions/upload-artifact@v6.0.0
      with:
        name: ${{ env.ARTIFACT_DIR }}
        path: ${{ env.ARTIFACT_DIR }}
    - name: Build Veil-Qt.app and .dmg
      # Native build machine is darwin, so `make deploy` takes the BUILD_DARWIN
      # path in Makefile.am and calls contrib/macdeploy/macdeployqtplus directly
      # with hdiutil (no cross-compile toolchain env vars needed). Output is
      # Veil-Core.dmg (from PACKAGE_NAME "Veil Core" in configure.ac), containing
      # Veil-Qt.app.
      run: make deploy
    - name: Upload Veil-Qt.dmg
      uses: actions/upload-artifact@v6.0.0
      with:
        name: macosx-arm64-Veil-Qt-dmg
        path: Veil-Core.dmg
